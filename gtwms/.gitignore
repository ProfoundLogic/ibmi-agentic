# codermake build stamps and logs -- generated, per-container, never committed.
# They record what was built into ONE library; committing them would make a
# fresh clone think objects already exist in a library it has never touched.
build/
tmp/
